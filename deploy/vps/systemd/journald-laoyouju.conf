# 劳有据 AI —— journald 日志上限（应用日志轮转方案）
# 安装位置：/etc/systemd/journald.conf.d/laoyouju.conf（需 mkdir -p）
# 应用日志走 journald（journalctl -u laoyouju-api）；Nginx 日志由系统自带 logrotate 轮转。
[Journal]
SystemMaxUse=64M
SystemKeepFree=256M
MaxFileSec=1week

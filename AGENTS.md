# AGENTS.md —— 项目规则（所有自动化与人类协作者必须遵守）

## 项目是什么

“劳有据 AI”：劳动问题，回答有依据。
- 产品形态：微信小程序 ＋ 可被搜索引擎收录的网页端；
- 两端共用同一套后端：腾讯云 CloudBase 云函数；
- 生成模型使用 DeepSeek API（仅服务端调用）。

## 不可动摇的约束

- DeepSeek 是生成模型：所有法律结论必须受检索到的资料约束，DeepSeek 只能根据检索材料生成答案。
- 不得生成不存在的法条、案例或 source_id；模型引用的 source_id 必须经过程序校验。
- API Key 永远不得进入客户端（网页、小程序、日志、测试快照、Git），只能存在于服务端环境变量。
- 不得抓取或复制付费数据库、公众号全文及未授权材料；内容只能来自审核后的仓库文件。
- 第一版不使用向量数据库、不接入付费 embedding API。
- 第一版不做原生 App、支付、文件上传、复杂登录系统和运营管理后台。
- 不存在有效依据时，必须明确说明“当前资料不足，无法可靠判断”。

## 工作纪律

- 每个任务开始前，先读：`AGENTS.md`、`README.md`、`docs/DECISIONS.md`、`docs/PROGRESS.md`。
- 每次只完成项目经理指定的阶段；不得擅自跨阶段、扩大需求或替项目经理作产品决策。
- 不得擅自创建云资源、部署、购买服务或修改远程状态。
- 不得修改、移动或删除与当前任务无关的用户文件；允许修改项目经理在当前阶段明确授权的项目文件。不得执行破坏性 Git 操作，如强推、reset、删除历史等。
- 所有完成声明必须附实际执行的测试命令和结果，不得只写“测试通过”。
- 遇到不确定项必须记录，不得用猜测填补。
- 报告中的 Blockers 只列真正阻止下一阶段的问题。

## 运行时与工具约束

- 云函数生产目标运行时为 Nodejs20.19。
- 本地允许 Node 24，但代码不得依赖仅有 Node 24 才支持的非标准特性。
- 不得配置第三方 npm 镜像；依赖一律使用官方 registry。
- 密钥管理、提示词注入防护等细节见 `docs/SECURITY.md`。

## 进程与端口操作安全（强制）

- 禁止按进程名称批量终止 node、pnpm、npm、corepack、npx 等进程（例如 `taskkill /F /IM node.exe`）；禁止使用会一次性终止全部 Node/Harness/工具链进程的命令。
- 只能停止“当前任务自己启动并已明确记录准确 PID”的进程；停止前必须核对 PID、命令行与监听端口，确认无法归属时一律不得终止。
- 凡由任务启动的测试 server，须通过 `finally`/`after` 或等价机制关闭自己创建的监听端口，不得遗留服务在硬编码端口上。
- 排障/验证过程中不得终止任何既有的 Node、Harness 或工具链进程；因误判而终止后必须在报告中如实记录。
